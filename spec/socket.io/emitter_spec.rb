require 'spec_helper'

describe Socket.io::Ruby::Emitter do
  it 'has a version number' do
    expect(Socket.io::Ruby::Emitter::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
